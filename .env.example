# ── Upstash Redis (required for Vercel deployment) ──────────────────
# Create a free Redis database at https://console.upstash.com
# Or enable the Upstash integration in Vercel dashboard → Integrations
# (env vars are then injected automatically)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Without these vars the app falls back to local filesystem storage,
# which works for development but NOT on Vercel (read-only filesystem).
